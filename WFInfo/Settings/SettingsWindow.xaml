<Window x:Class="WFInfo.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wfInfo="clr-namespace:WFInfo"
        xmlns:settings="clr-namespace:WFInfo.Settings"
        xmlns:components="clr-namespace:WFInfo.Components"
        xmlns:screenshot="clr-namespace:WFInfo.Services.Screenshot"
        mc:Ignorable="d"
        Title="Settings"
        MaxWidth="334"
        MinWidth="334"
        Width="334"
        Height="634"
        BorderBrush="#FF707070"
        WindowStyle="None"
        FontSize="16"
        d:DataContext="{d:DesignInstance Type=settings:SettingsWindow}"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <ObjectDataProvider x:Key="enumWFTheme" MethodName="GetValues" ObjectType="{x:Type wfInfo:WFtheme}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="wfInfo:WFtheme"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="enumHDRSupport" MethodName="GetValues" ObjectType="{x:Type screenshot:HdrSupportEnum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="screenshot:HdrSupportEnum"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <Grid MouseDown="MouseDown"
          PreviewMouseDown="ActivationMouseDown"
          Background="#FF1B1B1B"
          MinWidth="334">
        <Grid.Resources>

            <Style x:Key="NestedBorder"
                   TargetType="Border">
                <Setter Property="BorderBrush"
                        Value="#FF646464" />
                <Setter Property="Padding"
                        Value="5, 2" />
                <Setter Property="BorderThickness"
                        Value="1" />
            </Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush"
                        Value="#FF646464" />
                <Setter Property="Padding"
                        Value="25, 5" />
                <Setter Property="BorderThickness"
                        Value="1" />
            </Style>
            <Style x:Key="AllTextBoxes"
                   BasedOn="{StaticResource baseStyle}"
                   TargetType="TextBox">
                <Setter Property="Background"
                        Value="#FF0F0F0F" />
                <Setter Property="BorderBrush"
                        Value="#FFB1D0D9" />
                <Setter Property="VerticalContentAlignment"
                        Value="Center" />
                <Setter Property="HorizontalContentAlignment"
                        Value="Center" />
                <Setter Property="Margin"
                        Value="0, 5" />
                <Setter Property="Padding"
                        Value="0,1,0,2.5" />
                <Setter Property="Cursor"
                        Value="Arrow" />
                <Setter Property="TextWrapping"
                        Value="Wrap" />
                <Setter Property="FontFamily"
                        Value="{StaticResource Roboto}" />
                <Setter Property="components:SelectTextOnFocus.Active"
                        Value="True" />
                <Setter Property="wfInfo:FocusAdvancement.AdvancesByEnterKey"
                        Value="True" />
            </Style>

            <Style TargetType="CheckBox"
                   BasedOn="{StaticResource checkBoxStyle}">
                <Setter Property="VerticalContentAlignment"
                        Value="Center" />
            </Style>
            <Style BasedOn="{StaticResource AllTextBoxes}"
                   TargetType="TextBox" />
            <Style x:Key="KeyBindTextboxStyle"
                   BasedOn="{StaticResource AllTextBoxes}"
                   TargetType="TextBox">
                <Setter Property="components:SelectTextOnFocus.Active"
                        Value="False" />
                <Style.Triggers>
                    <Trigger Property="IsFocused"
                             Value="True">
                        <Setter Property="Foreground"
                                Value="Transparent">
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <components:NegateIntConverter x:Key="NegateIntConverter" />
            <components:KeyStringConverter x:Key="KeyStringConverter" />
            <components:BoolVisibilityConverter x:Key="BoolVisibilityConverter" />
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid VerticalAlignment="Top"
              Grid.Row="0"
              Grid.ColumnSpan="2"
              Grid.Column="0">
            <Rectangle Fill="#FF0F0F0F"
                       Stroke="#FF646464" />
            <DockPanel>
                <Image HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       Width="26"
                       DockPanel.Dock="Left"
                       Margin="2,0,5,1"
                       Source="../Resources/WFLogo.png" />
                <Label MouseLeftButtonDown="Hide"
                       Content="x"
                       Width="30"
                       Style="{StaticResource Label_Button}"
                       VerticalContentAlignment="Stretch"
                       DockPanel.Dock="Right" />
                <TextBlock Text="Settings"
                           VerticalAlignment="Center"
                           FontSize="16"
                           FontFamily="{StaticResource Roboto_Black}"
                           FontWeight="Bold" />
            </DockPanel>
        </Grid>
        <ScrollViewer Grid.Row="1"
                      Grid.Column="0">
            <StackPanel Orientation="Vertical">
                <Border DockPanel.Dock="Top"
                        Padding="15 5 25 5">
                    <DockPanel>
                        <Grid DockPanel.Dock="Bottom"
                              HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <RadioButton x:Name="WindowRadio"
                                         Grid.Column="1"
                                         Content="Window"
                                         VerticalContentAlignment="Center"
                                         Checked="WindowChecked"
                                         Background="#FFB1D0D9"
                                         BorderBrush="#FF0F0F0F"
                                         HorizontalAlignment="Center">
                                <RadioButton.ToolTip><ToolTip><TextBlock Text="Display rewards in a separate window" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></RadioButton.ToolTip>
                            </RadioButton>
                            <RadioButton x:Name="OverlayRadio"
                                         Grid.Column="2"
                                         Content="Overlay"
                                         VerticalContentAlignment="Center"
                                         Checked="OverlayChecked"
                                         Background="#FFB1D0D9"
                                         BorderBrush="#FF0F0F0F">
                                <RadioButton.ToolTip><ToolTip><TextBlock Text="Display rewards on top of Warframe" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></RadioButton.ToolTip>
                            </RadioButton>
                            <RadioButton x:Name="LightRadio"
                                         Grid.Column="0"
                                         Content="Light"
                                         VerticalContentAlignment="Center"
                                         Checked="LightRadioChecked"
                                         Background="#FFB1D0D9"
                                         BorderBrush="#FF0F0F0F">
                                <RadioButton.ToolTip><ToolTip><TextBlock Text="Do not display any window at all" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></RadioButton.ToolTip>
                            </RadioButton>
                        </Grid>
                        <Label Content="Rewards display"
                               DockPanel.Dock="Top"
                               HorizontalAlignment="Center"
                               FontFamily="{StaticResource Roboto}"
                               Margin="0,0,-1,0" />
                    </DockPanel>
                </Border>
                <Border DockPanel.Dock="Top"
                        Padding="15 5 25 5">
                    <UniformGrid>
                        <Label Content="Display time" />
                        <TextBox x:Name="Displaytime_number_box"
                                 Text="{Binding SettingsViewModel.Delay, Mode=TwoWay}"
                                 components:NumericOnlyEntry.RegexFilter="^[0-9\.,]+$">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="The time in milliseconds the overlays will be displayed on screen" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>

                        <CheckBox x:Name="HighlightCheckbox"
                                  Content="Highlight"
                                  IsChecked="{Binding SettingsViewModel.HighlightRewards, Mode=TwoWay}">
                            <CheckBox.ToolTip><ToolTip><TextBlock Text="Highlight the best rewards on the overlay." TextWrapping="Wrap" MaxWidth="300"/></ToolTip></CheckBox.ToolTip>
                        </CheckBox>
                        <CheckBox x:Name="HighContrastCheckbox"
                                  Content="High contrast"
                                  IsChecked="{Binding SettingsViewModel.HighContrast, Mode=TwoWay}">
                            <CheckBox.ToolTip><ToolTip><TextBlock Text="Enable High contrast for the overlay." TextWrapping="Wrap" MaxWidth="300"/></ToolTip></CheckBox.ToolTip>
                        </CheckBox>
                    </UniformGrid>
                </Border>
                <Border x:Name="Overlay_sliders"
                        DockPanel.Dock="Top"
                        Padding="15 5 25 5">
                    <DockPanel LastChildFill="False">
                        <Label Content="Overlay Offset"
                               DockPanel.Dock="Top"
                               HorizontalAlignment="Center"
                               FontFamily="{StaticResource Roboto}"
                               Margin="0,0,-1,0" />
                        <DockPanel LastChildFill="False"
                                   DockPanel.Dock="Top">
                            <Label Content="X"
                                   DockPanel.Dock="Left"
                                   HorizontalContentAlignment="Center"
                                   Width="32">
                                <Label.ToolTip><ToolTip><TextBlock Text="The X Offset of the overlay. Positive values shift the overlay to the right." TextWrapping="Wrap" MaxWidth="300"/></ToolTip></Label.ToolTip>
                            </Label>
                            <TextBox x:Name="OverlayXOffset_number_box"
                                     DockPanel.Dock="Left"
                                     TextWrapping="Wrap"
                                     Text="{Binding SettingsViewModel.OverlayXOffsetValue, Mode=TwoWay}"
                                     components:NumericOnlyEntry.RegexFilter="^-?[0-9]*$"
                                     Width="90">
                                <TextBox.ToolTip><ToolTip><TextBlock Text="The X Offset of the overlay. Positive values shift the overlay to the right." TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                            </TextBox>

                            <TextBox x:Name="OverlayYOffset_number_box"
                                     DockPanel.Dock="Right"
                                     Width="90"
                                     Text="{Binding SettingsViewModel.OverlayYOffsetValue, Mode=TwoWay
                             , Converter={StaticResource NegateIntConverter}}"
                                     components:NumericOnlyEntry.RegexFilter="^-?[0-9]*$">
                                <TextBox.ToolTip><ToolTip><TextBlock Text="The Y Offset of the overlay. Positive values shift the overlay towards the top." TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                            </TextBox>
                            <Label Content="Y"
                                   DockPanel.Dock="Right"
                                   HorizontalContentAlignment="Center"
                                   Width="32">
                                <Label.ToolTip><ToolTip><TextBlock Text="The Y Offset of the overlay. Positive values shift the overlay towards the top." TextWrapping="Wrap" MaxWidth="300"/></ToolTip></Label.ToolTip>
                            </Label>
                        </DockPanel>
                    </DockPanel>
                </Border>
                <Border DockPanel.Dock="Top" 
                        Padding="15 5 25 5">
                    <UniformGrid>
                        <Label Content="Min Width (snap)"/>
                        <TextBox x:Name="MinOverlayWidth_number_box"
                                 Text="{Binding SettingsViewModel.MinOverlayWidth, Mode=TwoWay}"
                                 components:NumericOnlyEntry.RegexFilter="^[0-9\.,]+$"
                                 VerticalAlignment="Top">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="The minimum width in pixels snap-it overlays will be displayed on screen" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>

                        <Label Content="Max Width (snap)"/>
                        <TextBox x:Name="MaxOverlayWidth_number_box"
                                 Text="{Binding SettingsViewModel.MaxOverlayWidth, Mode=TwoWay}"
                                 components:NumericOnlyEntry.RegexFilter="^[0-9\.,]+$"
                                 VerticalAlignment="Top">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="The maximum width in pixels snap-it overlays will be displayed on screen" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>

                    </UniformGrid>
                </Border>
                <Border DockPanel.Dock="Top" 
                        Padding="15 5 25 5">
                    <UniformGrid>
                        <Label Content="Display time (snap)"/>
                        <TextBox x:Name="SnapItDisplaytime_number_box"
                                 Text="{Binding SettingsViewModel.SnapItDelay, Mode=TwoWay}"
                                 components:NumericOnlyEntry.RegexFilter="^[0-9\.,]+$"
                                 VerticalAlignment="Top">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="The time in milliseconds the overlays will be displayed on screen" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>

                        <CheckBox x:Name="SnapItemCountCheckbox"
                                  Content="Count Item"
                                  IsChecked="{Binding SettingsViewModel.DoSnapItCount, Mode=TwoWay}">
                            <CheckBox.ToolTip><ToolTip><TextBlock Text="Scan for item amount using Snap-It" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></CheckBox.ToolTip>
                        </CheckBox>

                        <CheckBox x:Name="SnapItThreadCheckbox"
                                  Content="Multithread Snap"
                                  IsChecked="{Binding SettingsViewModel.SnapMultiThreaded, Mode=TwoWay}">
                            <CheckBox.ToolTip><ToolTip><TextBlock Text="Speed up snap-it using multithreading" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></CheckBox.ToolTip>
                        </CheckBox>

                    </UniformGrid>
                </Border>
                <Border DockPanel.Dock="Top"
                        Padding="15 5 25 5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Label Content="Activation key"
                               Grid.Row="0"
                               Grid.Column="0" />
                        <TextBox x:Name="Activation_key_box"
                                 Style="{StaticResource KeyBindTextboxStyle}"
                                 Grid.Row="0"
                                 Grid.Column="1"
                                 Text="{Binding SettingsViewModel.ActivationKeyDisplay, Mode=OneWay}"
                                 IsReadOnly="True"
                                 KeyUp="ActivationUp"
                                 VerticalAlignment="Top">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="Key to activate the processing. Use modifiers below to achieve other actions" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>
                        <TextBlock x:Name="hidden"
                                   Focusable="True"
                                   Visibility="Visible"
                                   IsEnabled="True"
                                   HorizontalAlignment="Left"
                                   TextWrapping="Wrap"
                                   Text=""
                                   VerticalAlignment="Top"
                                   Height="1"
                                   Width="1"
                                   RenderTransformOrigin="0.5,0.5"
                                   d:IsHidden="True" />
                        <Label Content="Search it modifier"
                               Grid.Row="1"
                               Grid.Column="0" />
                        <TextBox x:Name="Searchit_key_box"
                                 Style="{StaticResource KeyBindTextboxStyle}"
                                 Grid.Row="1"
                                 Grid.Column="1"
                                 IsReadOnly="True"
                                 KeyUp="Searchit_key_box_KeyUp"
                                 Text="{Binding SettingsViewModel.SearchItModifierKey,
                                Converter={StaticResource KeyStringConverter}}"
                                 VerticalAlignment="Top">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="Modifier to hold down to start search it" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>
                        <Label Content="Snap it modifier"
                               Grid.Row="2"
                               Grid.Column="0" />
                        <TextBox x:Name="Snapit_key_box"
                                 Style="{StaticResource KeyBindTextboxStyle}"
                                 Grid.Row="2"
                                 Grid.Column="1"
                                 Text="{Binding SettingsViewModel.SnapitModifierKey,
                                Converter={StaticResource KeyStringConverter}}"
                                 KeyUp="Snapit_key_box_KeyUp"
                                 IsReadOnly="True"
                                 VerticalAlignment="Top">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="Modifier to hold down to start Snap it" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>
                        <Label Content="Master it modifier"
                               Grid.Row="3"
                               Grid.Column="0" />
                        <TextBox x:Name="Masterit_key_box"
                                 Style="{StaticResource KeyBindTextboxStyle}"
                                 Grid.Row="3"
                                 Grid.Column="1"
                                 Text="{Binding SettingsViewModel.MasterItModifierKey,
                                Converter={StaticResource KeyStringConverter}}"
                                 IsReadOnly="True"
                                 KeyUp="Masterit_key_box_KeyUp"
                                 VerticalAlignment="Top">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="Modifier to hold down to start Master it" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>
                    </Grid>
                </Border>
                <Border DockPanel.Dock="Top"
                        Width="334"
                        Padding="15 5 25 5">
                    <UniformGrid>
                        <Label Content="Efficiency min" />
                        <Label Content="Efficiency max" />
                        <TextBox Margin="0, 0, 20, 0"
                                 Text="{Binding SettingsViewModel.MinimumEfficiencyValue, Mode=TwoWay}"
                                 components:NumericOnlyEntry.RegexFilter="^[0-9\.,]+$"
                                 AutoWordSelection="True"
                                 VerticalAlignment="Top">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="Anything below this efficiency will be marked as red in snapit" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>
                        <TextBox Text="{Binding SettingsViewModel.MaximumEfficiencyValue, Mode=TwoWay}"
                                 components:NumericOnlyEntry.RegexFilter="^[0-9\.,]+$"
                                 Margin="0, 0, 20, 0"
                                 VerticalAlignment="Top">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="Anything above this efficiency will be marked as green in snapit" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>
                    </UniformGrid>
                </Border>
                <Border DockPanel.Dock="Top"
                        Padding="15 5 25 5">
                    <UniformGrid Rows="1">
                        <Label Content="UI Scale">
                            <Label.ToolTip><ToolTip><TextBlock Text="In-game UI scale override (50-100). 0 = auto (uses EE.cfg or heuristics)." TextWrapping="Wrap" MaxWidth="300"/></ToolTip></Label.ToolTip>
                        </Label>
                        <TextBox x:Name="uiScaling_textbox"
                                 Text="{Binding SettingsViewModel.UserUiScaling, Mode=TwoWay}"
                                 components:NumericOnlyEntry.RegexFilter="^[0-9]+$">
                            <TextBox.ToolTip><ToolTip><TextBlock Text="0 = auto, 50-100 = custom UI scale percentage" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></TextBox.ToolTip>
                        </TextBox>
                    </UniformGrid>
                </Border>
                <Border Padding="0"
                        DockPanel.Dock="Top">
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="6*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="Theme" Grid.Column="0"/>
                            <Label x:Name="ConfigureTheme_button" Grid.Column="1" MouseLeftButtonDown="ConfigureTheme_button_MouseLeftButtonDown" Margin="0,5,10,5" Padding="3"  HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Height="20" VerticalAlignment="Top" HorizontalAlignment="Right" Width="20">
                                <Label.ToolTip><ToolTip><TextBlock Text="Configure Custom Theme" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></Label.ToolTip>
                                <Label.Style>
                                    <Style>
                                        <Setter Property="ContentControl.Content">
                                            <Setter.Value>
                                                <Image Source="../Resources/Settings.png" Stretch="Uniform" />
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="UIElement.IsMouseOver" Value="True"/>
                                                    <Condition Property="UIElement.IsEnabled" Value="True"/>
                                                </MultiTrigger.Conditions>
                                                <Setter Property="ContentControl.Content">
                                                    <Setter.Value>
                                                        <Image Source="../Resources/Settings_h.png" Stretch="Uniform" />
                                                    </Setter.Value>
                                                </Setter>
                                            </MultiTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Label.Style>
                            </Label>
                            <ComboBox 
                                          Grid.Column="2"
                                          x:Name="themeSelectionComboBox"
                                          FontSize="14"
                                          FontFamily="{DynamicResource Roboto}"
                                          Background="#FF1B1B1B"
                                          BorderBrush="#FF0F0F0F"
                                          ItemsSource="{Binding Source={StaticResource enumWFTheme}}"
                                          SelectedItem="{Binding SettingsViewModel.ThemeSelection}"
                                          Margin="5,4" Template="{DynamicResource ComboBoxTemplate}" Style="{DynamicResource ComboBoxStyle1}" SelectionChanged="themeSelectionComboBox_SelectionChanged"/>

                            <Label Visibility="{Binding SettingsViewModel.OsSupportsHDR, Converter={StaticResource BoolVisibilityConverter}}" Content="HDR" Grid.Column="0" Grid.Row="1"/>
                            <Label Visibility="{Binding SettingsViewModel.OsSupportsHDR, Converter={StaticResource BoolVisibilityConverter}}" Grid.Row="1" Grid.Column="1" Margin="0,5,10,5" Padding="3"  HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Height="20" VerticalAlignment="Top" HorizontalAlignment="Right" Width="20">
                                <Label.ToolTip><ToolTip><TextBlock Text="Auto - Detect based on game settings&#x0a;On - Always use HDR capture (will slightly change colors) &#x0a;Off - Always use SDR capture (will result in overblown HDR screenshots) &#x0a;&#x0a;It is recommended to use a custom theme with HDR enabled, this mode only compresses the dynamic color range." TextWrapping="Wrap" MaxWidth="300"/></ToolTip></Label.ToolTip>
                                <Label.Style>
                                    <Style>
                                        <Setter Property="ContentControl.Content">
                                            <Setter.Value>
                                                <Image Source="../Resources/info.png" Stretch="Uniform" />
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="UIElement.IsMouseOver" Value="True"/>
                                                    <Condition Property="UIElement.IsEnabled" Value="True"/>
                                                </MultiTrigger.Conditions>
                                                <Setter Property="ContentControl.Content">
                                                    <Setter.Value>
                                                        <Image Source="../Resources/info_h.png" Stretch="Uniform" />
                                                    </Setter.Value>
                                                </Setter>
                                            </MultiTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Label.Style>
                            </Label>
                            <ComboBox 
                                          Grid.Column="2"
                                          Grid.Row="1"
                                          Visibility="{Binding SettingsViewModel.OsSupportsHDR, Converter={StaticResource BoolVisibilityConverter}}"
                                          x:Name="hdrSelectionComboBox"
                                          FontSize="14"
                                          FontFamily="{DynamicResource Roboto}"
                                          Background="#FF1B1B1B"
                                          BorderBrush="#FF0F0F0F"
                                          ItemsSource="{Binding Source={StaticResource enumHDRSupport}}"
                                          SelectedItem="{Binding SettingsViewModel.HdrSupport}"
                                          Margin="5,4" Template="{DynamicResource ComboBoxTemplate}" Style="{DynamicResource ComboBoxStyle1}">

                            </ComboBox>
                        </Grid>
                        <Label MouseLeftButtonDown="ClickCreateDebug"
                               Padding="5"
                               DockPanel.Dock="Right"
                               Content="Create debug zip"
                               FontWeight="Regular"
                               Style="{StaticResource Label_Button}" />
                        <Border Style="{StaticResource NestedBorder}"
                                DockPanel.Dock="Top">
                            <UniformGrid DockPanel.Dock="Top"
                                         Rows="1">

                                <Label Content="Locale"
                                       DockPanel.Dock="Left"
                                       FontSize="16"
                                       FontWeight="Regular" />
                                <ComboBox SelectionChanged="localeComboboxSelectionChanged"
                                          DockPanel.Dock="Right"
                                          x:Name="localeCombobox"
                                          FontSize="14"
                                          FontFamily="{DynamicResource Roboto}"
                                          Background="#FF1B1B1B"
                                          BorderBrush="#FF0F0F0F"
                                          Margin="0,4" Template="{DynamicResource ComboBoxTemplate}" Style="{DynamicResource ComboBoxStyle1}">
                                    <ComboBoxItem Tag="en"
                                                  Content="English"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="fr"
                                                  Content="Français"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="it"
                                                  Content="Italiano"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="de"
                                                  Content="Deutsch"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="es"
                                                  Content="Español"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="pt"
                                                  Content="Português-Br"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="ru"
                                                  Content="Русский"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="pl"
                                                  Content="Polski"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="uk"
                                                  Content="Українська"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <!-- <ComboBoxItem Tag="tr"
                                                  Content="Türkçe"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" /> -->
                                    <!-- <ComboBoxItem Tag="ja"
                                                  Content="日本語"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" /> -->
                                    <ComboBoxItem Tag="zh-hans"
                                                  Content="简体中文"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="zh-hant"
                                                  Content="繁體中文"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <ComboBoxItem Tag="ko"
                                                  Content="한국어"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" />
                                    <!-- <ComboBoxItem Tag="th"
                                                  Content="ไทย"
                                                  FontSize="14"
                                                  Background="#FF1B1B1B" /> -->
                                </ComboBox>
                            </UniformGrid>
                        </Border>
                        <Border DockPanel.Dock="Top"
                                Style="{StaticResource NestedBorder}">
                            <CheckBox x:Name="clipboardCheckbox"
                                      Content="Clipboard"
                                      IsChecked="{Binding SettingsViewModel.Clipboard, Mode=TwoWay}">
                                <CheckBox.ToolTip><ToolTip><TextBlock Text="Copy the results from the OCR over to the clipboard for easy pasting into chat" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></CheckBox.ToolTip>
                            </CheckBox>
                        </Border>
                        <Border DockPanel.Dock="Top"
                                Style="{StaticResource NestedBorder}">
                            <UniformGrid Rows="2">

                                <CheckBox x:Name="autoCheckbox"
                                          Content="Auto"
                                          Click="AutoClicked">
                                    <CheckBox.ToolTip><ToolTip><TextBlock Text="Automatically detects when the relic screen is visible." TextWrapping="Wrap" MaxWidth="300"/></ToolTip></CheckBox.ToolTip>
                                </CheckBox>
                                <CheckBox x:Name="Autolist"
                                          Content="Auto List"
                                          IsChecked="{Binding SettingsViewModel.AutoList, Mode=TwoWay}">
                                    <CheckBox.ToolTip><ToolTip><TextBlock Text="Spawns a listing dialogue whenever the end of mission is detected, requires auto" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></CheckBox.ToolTip>
                                </CheckBox>
                                <CheckBox x:Name="Autocsv"
                                          Content="Auto CSV"
                                          IsChecked="{Binding SettingsViewModel.AutoCSV, Mode=TwoWay}">
                                    <CheckBox.ToolTip><ToolTip><TextBlock Text="Save reward options and choice to CSV file in application directory (%appdata%\WFInfo)" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></CheckBox.ToolTip>
                                </CheckBox>
                                <CheckBox x:Name="Autoadd"
                                          Content="Auto Add"
                                          IsChecked="{Binding SettingsViewModel.AutoCount, Mode=TwoWay}">
                                    <CheckBox.ToolTip><ToolTip><TextBlock Text="Spawns a dialogue for increasing owned item count whenever the end of mission is detected, requires auto" TextWrapping="Wrap" MaxWidth="300"/></ToolTip></CheckBox.ToolTip>
                                </CheckBox>
                            </UniformGrid>
                        </Border>
                        <Border DockPanel.Dock="Top"
                                Style="{StaticResource NestedBorder}">
                            <UniformGrid Rows="2">
                                <Label Content="Initial delay (ms)">
                                    <Label.ToolTip>
                                        <ToolTip><TextBlock Text="Time in milliseconds to wait before polling for reward screen, only used when Auto is enabled (default: 500)" TextWrapping="Wrap" MaxWidth="300"/></ToolTip>
                                    </Label.ToolTip>
                                </Label>
                                <TextBox x:Name="FixedAutoDelay_number_box"
                                         Text="{Binding SettingsViewModel.FixedAutoDelay, Mode=TwoWay}"
                                         components:NumericOnlyEntry.RegexFilter="^[0-9]+$">
                                    <TextBox.ToolTip>
                                        <ToolTip><TextBlock Text="Time in milliseconds to wait before polling for reward screen, only used when Auto is enabled (default: 500)" TextWrapping="Wrap" MaxWidth="300"/></ToolTip>
                                    </TextBox.ToolTip>
                                </TextBox>
                                <Label Content="Poll interval (ms)">
                                    <Label.ToolTip>
                                        <ToolTip><TextBlock Text="Time in milliseconds between theme detection polls, only used when Auto is enabled (default: 500)" TextWrapping="Wrap" MaxWidth="300"/></ToolTip>
                                    </Label.ToolTip>
                                </Label>
                                <TextBox x:Name="AutoDelay_number_box"
                                         Text="{Binding SettingsViewModel.AutoDelay, Mode=TwoWay}"
                                         components:NumericOnlyEntry.RegexFilter="^[0-9]+$">
                                    <TextBox.ToolTip>
                                        <ToolTip><TextBlock Text="Time in milliseconds between theme detection polls, only used when Auto is enabled (default: 500)" TextWrapping="Wrap" MaxWidth="300"/></ToolTip>
                                    </TextBox.ToolTip>
                                </TextBox>
                            </UniformGrid>
                        </Border>
                    </DockPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
